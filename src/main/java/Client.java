import java.util.List;
import java.util.Arrays;
import org.sql2o.*;


public class Client {
  private int mId;


  public Client() {

  }

  // @Override
  // public boolean equals(Object otherClient){
  //   if (!(otherClient instanceof Client)) {
  //     return false;
  //   } else {
  //     Client newClient = (Client) otherClient;
  //     return this.getClientName().equals(newClient.getClientName()) && this.getId() == newClient.getId();
  //   }
  // }
}
